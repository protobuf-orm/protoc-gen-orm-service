// Code generated by protoc-gen-orm-service. DO NOT EDIT.
// source: apptest/field.proto

edition = "2023";

package apptest;

import "apptest/field.proto";
import "patch/patch.proto";
import "google/protobuf/struct.proto";

option go_package = "github.com/protobuf-orm/protoc-gen-orm-service/internal/apptest";

service ValueFieldService {
	// Add creates a new ValueField
	rpc Add(ValueFieldAddRequest) returns (ValueField);
	// Get retrieves a ValueField
	rpc Get(ValueFieldGetRequest) returns (ValueField);
	// Patch updates an existing ValueField
	rpc Patch(ValueFieldPatchRequest) returns (ValueField);
	// Apply applies a patch document to an existing ValueField
	rpc Apply(ValueFieldApplyRequest) returns (ValueField);
	// Erase deletes a ValueField
	rpc Erase(ValueFieldRef) returns (ValueFieldEraseResponse);
}

message ValueFieldAddRequest {
	string id = 1;
	double implicit_f64 = 16 [
		features.field_presence = IMPLICIT
	];
	float implicit_f32 = 17 [
		features.field_presence = IMPLICIT
	];
	int32 implicit_i32 = 18 [
		features.field_presence = IMPLICIT
	];
	int64 implicit_i64 = 19 [
		features.field_presence = IMPLICIT
	];
	uint32 implicit_u32 = 20 [
		features.field_presence = IMPLICIT
	];
	uint64 implicit_u64 = 21 [
		features.field_presence = IMPLICIT
	];
	sint32 implicit_si32 = 22 [
		features.field_presence = IMPLICIT
	];
	sint64 implicit_si64 = 23 [
		features.field_presence = IMPLICIT
	];
	fixed32 implicit_fi32 = 24 [
		features.field_presence = IMPLICIT
	];
	fixed64 implicit_fi64 = 25 [
		features.field_presence = IMPLICIT
	];
	sfixed32 implicit_sfi32 = 26 [
		features.field_presence = IMPLICIT
	];
	sfixed64 implicit_sfi64 = 27 [
		features.field_presence = IMPLICIT
	];
	bool implicit_bool = 28 [
		features.field_presence = IMPLICIT
	];
	string implicit_string = 29 [
		features.field_presence = IMPLICIT
	];
	bytes implicit_bytes = 30 [
		features.field_presence = IMPLICIT
	];
	Level implicit_enum = 31 [
		features.field_presence = IMPLICIT
	];
	double explicit_f64 = 48;
	float explicit_f32 = 49;
	int32 explicit_i32 = 50;
	int64 explicit_i64 = 51;
	uint32 explicit_u32 = 52;
	uint64 explicit_u64 = 53;
	sint32 explicit_si32 = 54;
	sint64 explicit_si64 = 55;
	fixed32 explicit_fi32 = 56;
	fixed64 explicit_fi64 = 57;
	sfixed32 explicit_sfi32 = 58;
	sfixed64 explicit_sfi64 = 59;
	bool explicit_bool = 60;
	string explicit_string = 61;
	bytes explicit_bytes = 62;
	Level explicit_enum = 63;
	repeated double implicit_f64s = 80;
	repeated float implicit_f32s = 81;
	repeated int32 implicit_i32s = 82;
	repeated int64 implicit_i64s = 83;
	repeated uint32 implicit_u32s = 84;
	repeated uint64 implicit_u64s = 85;
	repeated sint32 implicit_si32s = 86;
	repeated sint64 implicit_si64s = 87;
	repeated fixed32 implicit_fi32s = 88;
	repeated fixed64 implicit_fi64s = 89;
	repeated sfixed32 implicit_sfi32s = 90;
	repeated sfixed64 implicit_sfi64s = 91;
	repeated bool implicit_bools = 92;
	repeated string implicit_strings = 93;
	repeated bytes implicit_bytess = 94;
	repeated Level implicit_levels = 95;
	double nullable_f64 = 112;
	float nullable_f32 = 113;
	int32 nullable_i32 = 114;
	int64 nullable_i64 = 115;
	uint32 nullable_u32 = 116;
	uint64 nullable_u64 = 117;
	sint32 nullable_si32 = 118;
	sint64 nullable_si64 = 119;
	fixed32 nullable_fi32 = 120;
	fixed64 nullable_fi64 = 121;
	sfixed32 nullable_sfi32 = 122;
	sfixed64 nullable_sfi64 = 123;
	bool nullable_bool = 124;
	string nullable_string = 125;
	bytes nullable_bytes = 126;
	Level nullable_level = 127;
	double implicit_f64_with_default = 144;
	float implicit_f32_with_default = 145;
	int32 implicit_i32_with_default = 146;
	int64 implicit_i64_with_default = 147;
	uint32 implicit_u32_with_default = 148;
	uint64 implicit_u64_with_default = 149;
	sint32 implicit_si32_with_default = 150;
	sint64 implicit_si64_with_default = 151;
	fixed32 implicit_fi32_with_default = 152;
	fixed64 implicit_fi64_with_default = 153;
	sfixed32 implicit_sfi32_with_default = 154;
	sfixed64 implicit_sfi64_with_default = 155;
	bool implicit_bool_with_default = 156;
	string implicit_string_with_default = 157;
	bytes implicit_bytes_with_default = 158;
	Level implicit_level_with_default = 159;
	double explicit_f64_with_default = 176;
	float explicit_f32_with_default = 177;
	int32 explicit_i32_with_default = 178;
	int64 explicit_i64_with_default = 179;
	uint32 explicit_u32_with_default = 180;
	uint64 explicit_u64_with_default = 181;
	sint32 explicit_si32_with_default = 182;
	sint64 explicit_si64_with_default = 183;
	fixed32 explicit_fi32_with_default = 184;
	fixed64 explicit_fi64_with_default = 185;
	sfixed32 explicit_sfi32_with_default = 186;
	sfixed64 explicit_sfi64_with_default = 187;
	bool explicit_bool_with_default = 188;
	string explicit_string_with_default = 189;
	bytes explicit_bytes_with_default = 190;
	Level explicit_level_with_default = 191;
	double implicit_immutable_f64 = 208 [
		features.field_presence = IMPLICIT
	];
	float implicit_immutable_f32 = 209 [
		features.field_presence = IMPLICIT
	];
	int32 implicit_immutable_i32 = 210 [
		features.field_presence = IMPLICIT
	];
	int64 implicit_immutable_i64 = 211 [
		features.field_presence = IMPLICIT
	];
	uint32 implicit_immutable_u32 = 212 [
		features.field_presence = IMPLICIT
	];
	uint64 implicit_immutable_u64 = 213 [
		features.field_presence = IMPLICIT
	];
	sint32 implicit_immutable_si32 = 214 [
		features.field_presence = IMPLICIT
	];
	sint64 implicit_immutable_si64 = 215 [
		features.field_presence = IMPLICIT
	];
	fixed32 implicit_immutable_fi32 = 216 [
		features.field_presence = IMPLICIT
	];
	fixed64 implicit_immutable_fi64 = 217 [
		features.field_presence = IMPLICIT
	];
	sfixed32 implicit_immutable_sfi32 = 218 [
		features.field_presence = IMPLICIT
	];
	sfixed64 implicit_immutable_sfi64 = 219 [
		features.field_presence = IMPLICIT
	];
	bool implicit_immutable_bool = 220 [
		features.field_presence = IMPLICIT
	];
	string implicit_immutable_string = 221 [
		features.field_presence = IMPLICIT
	];
	bytes implicit_immutable_bytes = 222 [
		features.field_presence = IMPLICIT
	];
	Level implicit_immutable_level = 223 [
		features.field_presence = IMPLICIT
	];
	double explicit_immutable_f64 = 240;
	float explicit_immutable_f32 = 241;
	int32 explicit_immutable_i32 = 242;
	int64 explicit_immutable_i64 = 243;
	uint32 explicit_immutable_u32 = 244;
	uint64 explicit_immutable_u64 = 245;
	sint32 explicit_immutable_si32 = 246;
	sint64 explicit_immutable_si64 = 247;
	fixed32 explicit_immutable_fi32 = 248;
	fixed64 explicit_immutable_fi64 = 249;
	sfixed32 explicit_immutable_sfi32 = 250;
	sfixed64 explicit_immutable_sfi64 = 251;
	bool explicit_immutable_bool = 252;
	string explicit_immutable_string = 253;
	bytes explicit_immutable_bytes = 254;
	Level explicit_immutable_level = 255;
}

message ValueFieldRef {
	oneof key {
		string id = 1;
	}
}

message ValueFieldSelect {
	bool all = 1;
	bool implicit_f64 = 16;
	bool implicit_f32 = 17;
	bool implicit_i32 = 18;
	bool implicit_i64 = 19;
	bool implicit_u32 = 20;
	bool implicit_u64 = 21;
	bool implicit_si32 = 22;
	bool implicit_si64 = 23;
	bool implicit_fi32 = 24;
	bool implicit_fi64 = 25;
	bool implicit_sfi32 = 26;
	bool implicit_sfi64 = 27;
	bool implicit_bool = 28;
	bool implicit_string = 29;
	bool implicit_bytes = 30;
	bool implicit_enum = 31;
	bool explicit_f64 = 48;
	bool explicit_f32 = 49;
	bool explicit_i32 = 50;
	bool explicit_i64 = 51;
	bool explicit_u32 = 52;
	bool explicit_u64 = 53;
	bool explicit_si32 = 54;
	bool explicit_si64 = 55;
	bool explicit_fi32 = 56;
	bool explicit_fi64 = 57;
	bool explicit_sfi32 = 58;
	bool explicit_sfi64 = 59;
	bool explicit_bool = 60;
	bool explicit_string = 61;
	bool explicit_bytes = 62;
	bool explicit_enum = 63;
	bool implicit_f64s = 80;
	bool implicit_f32s = 81;
	bool implicit_i32s = 82;
	bool implicit_i64s = 83;
	bool implicit_u32s = 84;
	bool implicit_u64s = 85;
	bool implicit_si32s = 86;
	bool implicit_si64s = 87;
	bool implicit_fi32s = 88;
	bool implicit_fi64s = 89;
	bool implicit_sfi32s = 90;
	bool implicit_sfi64s = 91;
	bool implicit_bools = 92;
	bool implicit_strings = 93;
	bool implicit_bytess = 94;
	bool implicit_levels = 95;
	bool nullable_f64 = 112;
	bool nullable_f32 = 113;
	bool nullable_i32 = 114;
	bool nullable_i64 = 115;
	bool nullable_u32 = 116;
	bool nullable_u64 = 117;
	bool nullable_si32 = 118;
	bool nullable_si64 = 119;
	bool nullable_fi32 = 120;
	bool nullable_fi64 = 121;
	bool nullable_sfi32 = 122;
	bool nullable_sfi64 = 123;
	bool nullable_bool = 124;
	bool nullable_string = 125;
	bool nullable_bytes = 126;
	bool nullable_level = 127;
	bool implicit_f64_with_default = 144;
	bool implicit_f32_with_default = 145;
	bool implicit_i32_with_default = 146;
	bool implicit_i64_with_default = 147;
	bool implicit_u32_with_default = 148;
	bool implicit_u64_with_default = 149;
	bool implicit_si32_with_default = 150;
	bool implicit_si64_with_default = 151;
	bool implicit_fi32_with_default = 152;
	bool implicit_fi64_with_default = 153;
	bool implicit_sfi32_with_default = 154;
	bool implicit_sfi64_with_default = 155;
	bool implicit_bool_with_default = 156;
	bool implicit_string_with_default = 157;
	bool implicit_bytes_with_default = 158;
	bool implicit_level_with_default = 159;
	bool explicit_f64_with_default = 176;
	bool explicit_f32_with_default = 177;
	bool explicit_i32_with_default = 178;
	bool explicit_i64_with_default = 179;
	bool explicit_u32_with_default = 180;
	bool explicit_u64_with_default = 181;
	bool explicit_si32_with_default = 182;
	bool explicit_si64_with_default = 183;
	bool explicit_fi32_with_default = 184;
	bool explicit_fi64_with_default = 185;
	bool explicit_sfi32_with_default = 186;
	bool explicit_sfi64_with_default = 187;
	bool explicit_bool_with_default = 188;
	bool explicit_string_with_default = 189;
	bool explicit_bytes_with_default = 190;
	bool explicit_level_with_default = 191;
	bool implicit_immutable_f64 = 208;
	bool implicit_immutable_f32 = 209;
	bool implicit_immutable_i32 = 210;
	bool implicit_immutable_i64 = 211;
	bool implicit_immutable_u32 = 212;
	bool implicit_immutable_u64 = 213;
	bool implicit_immutable_si32 = 214;
	bool implicit_immutable_si64 = 215;
	bool implicit_immutable_fi32 = 216;
	bool implicit_immutable_fi64 = 217;
	bool implicit_immutable_sfi32 = 218;
	bool implicit_immutable_sfi64 = 219;
	bool implicit_immutable_bool = 220;
	bool implicit_immutable_string = 221;
	bool implicit_immutable_bytes = 222;
	bool implicit_immutable_level = 223;
	bool explicit_immutable_f64 = 240;
	bool explicit_immutable_f32 = 241;
	bool explicit_immutable_i32 = 242;
	bool explicit_immutable_i64 = 243;
	bool explicit_immutable_u32 = 244;
	bool explicit_immutable_u64 = 245;
	bool explicit_immutable_si32 = 246;
	bool explicit_immutable_si64 = 247;
	bool explicit_immutable_fi32 = 248;
	bool explicit_immutable_fi64 = 249;
	bool explicit_immutable_sfi32 = 250;
	bool explicit_immutable_sfi64 = 251;
	bool explicit_immutable_bool = 252;
	bool explicit_immutable_string = 253;
	bool explicit_immutable_bytes = 254;
	bool explicit_immutable_level = 255;
}

message ValueFieldGetRequest {
	ValueFieldRef ref = 1;
	ValueFieldSelect select = 2;
}

message ValueFieldPatchRequest {
	ValueFieldRef ref = 1;
	double implicit_f64 = 32;
	float implicit_f32 = 34;
	int32 implicit_i32 = 36;
	int64 implicit_i64 = 38;
	uint32 implicit_u32 = 40;
	uint64 implicit_u64 = 42;
	sint32 implicit_si32 = 44;
	sint64 implicit_si64 = 46;
	fixed32 implicit_fi32 = 48;
	fixed64 implicit_fi64 = 50;
	sfixed32 implicit_sfi32 = 52;
	sfixed64 implicit_sfi64 = 54;
	bool implicit_bool = 56;
	string implicit_string = 58;
	bytes implicit_bytes = 60;
	Level implicit_enum = 62;
	double explicit_f64 = 96;
	// Clear explicit_f64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_f64 clears.
	bool explicit_f64_null = 97;
	float explicit_f32 = 98;
	// Clear explicit_f32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_f32 clears.
	bool explicit_f32_null = 99;
	int32 explicit_i32 = 100;
	// Clear explicit_i32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_i32 clears.
	bool explicit_i32_null = 101;
	int64 explicit_i64 = 102;
	// Clear explicit_i64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_i64 clears.
	bool explicit_i64_null = 103;
	uint32 explicit_u32 = 104;
	// Clear explicit_u32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_u32 clears.
	bool explicit_u32_null = 105;
	uint64 explicit_u64 = 106;
	// Clear explicit_u64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_u64 clears.
	bool explicit_u64_null = 107;
	sint32 explicit_si32 = 108;
	// Clear explicit_si32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_si32 clears.
	bool explicit_si32_null = 109;
	sint64 explicit_si64 = 110;
	// Clear explicit_si64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_si64 clears.
	bool explicit_si64_null = 111;
	fixed32 explicit_fi32 = 112;
	// Clear explicit_fi32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_fi32 clears.
	bool explicit_fi32_null = 113;
	fixed64 explicit_fi64 = 114;
	// Clear explicit_fi64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_fi64 clears.
	bool explicit_fi64_null = 115;
	sfixed32 explicit_sfi32 = 116;
	// Clear explicit_sfi32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_sfi32 clears.
	bool explicit_sfi32_null = 117;
	sfixed64 explicit_sfi64 = 118;
	// Clear explicit_sfi64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_sfi64 clears.
	bool explicit_sfi64_null = 119;
	bool explicit_bool = 120;
	// Clear explicit_bool instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_bool clears.
	bool explicit_bool_null = 121;
	string explicit_string = 122;
	// Clear explicit_string instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_string clears.
	bool explicit_string_null = 123;
	bytes explicit_bytes = 124;
	// Clear explicit_bytes instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_bytes clears.
	bool explicit_bytes_null = 125;
	Level explicit_enum = 126;
	// Clear explicit_enum instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_enum clears.
	bool explicit_enum_null = 127;
	repeated double implicit_f64s = 160;
	repeated float implicit_f32s = 162;
	repeated int32 implicit_i32s = 164;
	repeated int64 implicit_i64s = 166;
	repeated uint32 implicit_u32s = 168;
	repeated uint64 implicit_u64s = 170;
	repeated sint32 implicit_si32s = 172;
	repeated sint64 implicit_si64s = 174;
	repeated fixed32 implicit_fi32s = 176;
	repeated fixed64 implicit_fi64s = 178;
	repeated sfixed32 implicit_sfi32s = 180;
	repeated sfixed64 implicit_sfi64s = 182;
	repeated bool implicit_bools = 184;
	repeated string implicit_strings = 186;
	repeated bytes implicit_bytess = 188;
	repeated Level implicit_levels = 190;
	double nullable_f64 = 224;
	// Clear nullable_f64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_f64 clears.
	bool nullable_f64_null = 225;
	float nullable_f32 = 226;
	// Clear nullable_f32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_f32 clears.
	bool nullable_f32_null = 227;
	int32 nullable_i32 = 228;
	// Clear nullable_i32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_i32 clears.
	bool nullable_i32_null = 229;
	int64 nullable_i64 = 230;
	// Clear nullable_i64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_i64 clears.
	bool nullable_i64_null = 231;
	uint32 nullable_u32 = 232;
	// Clear nullable_u32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_u32 clears.
	bool nullable_u32_null = 233;
	uint64 nullable_u64 = 234;
	// Clear nullable_u64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_u64 clears.
	bool nullable_u64_null = 235;
	sint32 nullable_si32 = 236;
	// Clear nullable_si32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_si32 clears.
	bool nullable_si32_null = 237;
	sint64 nullable_si64 = 238;
	// Clear nullable_si64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_si64 clears.
	bool nullable_si64_null = 239;
	fixed32 nullable_fi32 = 240;
	// Clear nullable_fi32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_fi32 clears.
	bool nullable_fi32_null = 241;
	fixed64 nullable_fi64 = 242;
	// Clear nullable_fi64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_fi64 clears.
	bool nullable_fi64_null = 243;
	sfixed32 nullable_sfi32 = 244;
	// Clear nullable_sfi32 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_sfi32 clears.
	bool nullable_sfi32_null = 245;
	sfixed64 nullable_sfi64 = 246;
	// Clear nullable_sfi64 instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_sfi64 clears.
	bool nullable_sfi64_null = 247;
	bool nullable_bool = 248;
	// Clear nullable_bool instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_bool clears.
	bool nullable_bool_null = 249;
	string nullable_string = 250;
	// Clear nullable_string instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_string clears.
	bool nullable_string_null = 251;
	bytes nullable_bytes = 252;
	// Clear nullable_bytes instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_bytes clears.
	bool nullable_bytes_null = 253;
	Level nullable_level = 254;
	// Clear nullable_level instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable_level clears.
	bool nullable_level_null = 255;
	double implicit_f64_with_default = 288;
	float implicit_f32_with_default = 290;
	int32 implicit_i32_with_default = 292;
	int64 implicit_i64_with_default = 294;
	uint32 implicit_u32_with_default = 296;
	uint64 implicit_u64_with_default = 298;
	sint32 implicit_si32_with_default = 300;
	sint64 implicit_si64_with_default = 302;
	fixed32 implicit_fi32_with_default = 304;
	fixed64 implicit_fi64_with_default = 306;
	sfixed32 implicit_sfi32_with_default = 308;
	sfixed64 implicit_sfi64_with_default = 310;
	bool implicit_bool_with_default = 312;
	string implicit_string_with_default = 314;
	bytes implicit_bytes_with_default = 316;
	Level implicit_level_with_default = 318;
	double explicit_f64_with_default = 352;
	// Clear explicit_f64_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_f64_with_default clears.
	bool explicit_f64_with_default_null = 353;
	float explicit_f32_with_default = 354;
	// Clear explicit_f32_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_f32_with_default clears.
	bool explicit_f32_with_default_null = 355;
	int32 explicit_i32_with_default = 356;
	// Clear explicit_i32_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_i32_with_default clears.
	bool explicit_i32_with_default_null = 357;
	int64 explicit_i64_with_default = 358;
	// Clear explicit_i64_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_i64_with_default clears.
	bool explicit_i64_with_default_null = 359;
	uint32 explicit_u32_with_default = 360;
	// Clear explicit_u32_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_u32_with_default clears.
	bool explicit_u32_with_default_null = 361;
	uint64 explicit_u64_with_default = 362;
	// Clear explicit_u64_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_u64_with_default clears.
	bool explicit_u64_with_default_null = 363;
	sint32 explicit_si32_with_default = 364;
	// Clear explicit_si32_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_si32_with_default clears.
	bool explicit_si32_with_default_null = 365;
	sint64 explicit_si64_with_default = 366;
	// Clear explicit_si64_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_si64_with_default clears.
	bool explicit_si64_with_default_null = 367;
	fixed32 explicit_fi32_with_default = 368;
	// Clear explicit_fi32_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_fi32_with_default clears.
	bool explicit_fi32_with_default_null = 369;
	fixed64 explicit_fi64_with_default = 370;
	// Clear explicit_fi64_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_fi64_with_default clears.
	bool explicit_fi64_with_default_null = 371;
	sfixed32 explicit_sfi32_with_default = 372;
	// Clear explicit_sfi32_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_sfi32_with_default clears.
	bool explicit_sfi32_with_default_null = 373;
	sfixed64 explicit_sfi64_with_default = 374;
	// Clear explicit_sfi64_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_sfi64_with_default clears.
	bool explicit_sfi64_with_default_null = 375;
	bool explicit_bool_with_default = 376;
	// Clear explicit_bool_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_bool_with_default clears.
	bool explicit_bool_with_default_null = 377;
	string explicit_string_with_default = 378;
	// Clear explicit_string_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_string_with_default clears.
	bool explicit_string_with_default_null = 379;
	bytes explicit_bytes_with_default = 380;
	// Clear explicit_bytes_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_bytes_with_default clears.
	bool explicit_bytes_with_default_null = 381;
	Level explicit_level_with_default = 382;
	// Clear explicit_level_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_level_with_default clears.
	bool explicit_level_with_default_null = 383;
}

message ValueFieldApplyRequest {
	ValueFieldRef ref = 1;
	patch.Patch patch = 2;
}

message ValueFieldEraseResponse {
	// Erased is whether this call is the one that erased the row.
	// 
	// False for a row that was already gone, was never there, or is out
	// of this caller's reach -- which are one answer on purpose, and the
	// reason the RPC does not fail instead.
	bool erased = 1;
}

service MessageFieldService {
	// Add creates a new MessageField
	rpc Add(MessageFieldAddRequest) returns (MessageField);
	// Get retrieves a MessageField
	rpc Get(MessageFieldGetRequest) returns (MessageField);
	// Patch updates an existing MessageField
	rpc Patch(MessageFieldPatchRequest) returns (MessageField);
	// Apply applies a patch document to an existing MessageField
	rpc Apply(MessageFieldApplyRequest) returns (MessageField);
	// Erase deletes a MessageField
	rpc Erase(MessageFieldRef) returns (MessageFieldEraseResponse);
}

message MessageFieldAddRequest {
	string id = 1;
	google.protobuf.Struct explicit = 48;
	repeated google.protobuf.Struct repeated = 80;
	google.protobuf.Struct nullable = 112;
	google.protobuf.Struct explicit_with_default = 176;
	google.protobuf.Struct explicit_immutable = 240;
}

message MessageFieldRef {
	oneof key {
		string id = 1;
	}
}

message MessageFieldSelect {
	bool all = 1;
	bool explicit = 48;
	bool repeated = 80;
	bool nullable = 112;
	bool explicit_with_default = 176;
	bool explicit_immutable = 240;
}

message MessageFieldGetRequest {
	MessageFieldRef ref = 1;
	MessageFieldSelect select = 2;
}

message MessageFieldPatchRequest {
	MessageFieldRef ref = 1;
	google.protobuf.Struct explicit = 96;
	// Clear explicit instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit clears.
	bool explicit_null = 97;
	repeated google.protobuf.Struct repeated = 160;
	google.protobuf.Struct nullable = 224;
	// Clear nullable instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and nullable clears.
	bool nullable_null = 225;
	google.protobuf.Struct explicit_with_default = 352;
	// Clear explicit_with_default instead of writing it.
	// It takes a field of its own because an unset value already means
	// "leave it alone", so no value could have meant NULL. It wins
	// outright: setting both this and explicit_with_default clears.
	bool explicit_with_default_null = 353;
}

message MessageFieldApplyRequest {
	MessageFieldRef ref = 1;
	patch.Patch patch = 2;
}

message MessageFieldEraseResponse {
	// Erased is whether this call is the one that erased the row.
	// 
	// False for a row that was already gone, was never there, or is out
	// of this caller's reach -- which are one answer on purpose, and the
	// reason the RPC does not fail instead.
	bool erased = 1;
}

service MapFieldService {
	// Add creates a new MapField
	rpc Add(MapFieldAddRequest) returns (MapField);
	// Get retrieves a MapField
	rpc Get(MapFieldGetRequest) returns (MapField);
	// Patch updates an existing MapField
	rpc Patch(MapFieldPatchRequest) returns (MapField);
	// Apply applies a patch document to an existing MapField
	rpc Apply(MapFieldApplyRequest) returns (MapField);
	// Erase deletes a MapField
	rpc Erase(MapFieldRef) returns (MapFieldEraseResponse);
}

message MapFieldAddRequest {
	string id = 1;
	map<string, string> implicit_string = 16;
	map<string, apptest.Level> implicit_enum = 17;
	map<string, google.protobuf.Struct> implicit_json = 18;
	map<string, string> implicit_string_with_default = 144;
	map<string, apptest.Level> implicit_enum_with_default = 145;
	map<string, google.protobuf.Struct> implicit_json_with_default = 146;
	map<string, string> implicit_immutable_string = 208;
	map<string, apptest.Level> implicit_immutable_enum = 209;
	map<string, google.protobuf.Struct> implicit_immutable_json = 210;
}

message MapFieldRef {
	oneof key {
		string id = 1;
	}
}

message MapFieldSelect {
	bool all = 1;
	bool implicit_string = 16;
	bool implicit_enum = 17;
	bool implicit_json = 18;
	bool implicit_string_with_default = 144;
	bool implicit_enum_with_default = 145;
	bool implicit_json_with_default = 146;
	bool implicit_immutable_string = 208;
	bool implicit_immutable_enum = 209;
	bool implicit_immutable_json = 210;
}

message MapFieldGetRequest {
	MapFieldRef ref = 1;
	MapFieldSelect select = 2;
}

message MapFieldPatchRequest {
	MapFieldRef ref = 1;
	map<string, string> implicit_string = 32;
	map<string, apptest.Level> implicit_enum = 34;
	map<string, google.protobuf.Struct> implicit_json = 36;
	map<string, string> implicit_string_with_default = 288;
	map<string, apptest.Level> implicit_enum_with_default = 290;
	map<string, google.protobuf.Struct> implicit_json_with_default = 292;
}

message MapFieldApplyRequest {
	MapFieldRef ref = 1;
	patch.Patch patch = 2;
}

message MapFieldEraseResponse {
	// Erased is whether this call is the one that erased the row.
	// 
	// False for a row that was already gone, was never there, or is out
	// of this caller's reach -- which are one answer on purpose, and the
	// reason the RPC does not fail instead.
	bool erased = 1;
}
